const FilterTitle = ({ title }: { title: string }) => {
  return (
    <h6 className="font-exo text-sm uppercase text-[#8A8D8B] font-semibold">
      {title}
    </h6>
  );
};

export default FilterTitle;
