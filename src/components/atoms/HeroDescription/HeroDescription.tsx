const HeroDescription = () => {
  return (
    <p className="text-center max-sm:text-sm text-black">
      Stop wasting time chasing unreliable artisans. Work with vetted hands on
      your projects. Track the job. Pay only when it’s complete
    </p>
  );
};

export default HeroDescription;
